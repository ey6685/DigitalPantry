extends layout_template

block content
    .col-md-12
        br
        br
        h1.mb-3 Welcome!
        br
        br
        table.table.table-dark  
            p
                h1 Next expiring ingredient: #{i_name} on #{(i_expire)}